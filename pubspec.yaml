name: jazone_monitoring_dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.5.0
  google_maps_flutter_web: ^0.5.7
  intl: ^0.19.0
  pdf: ^3.11.3
  printing: ^5.14.2
  geolocator: ^14.0.2
  google_maps_flutter: ^2.14.2
  firebase_functions: ^0.0.1-dev
  latlong2: ^0.9.1
  flutter_map: ^8.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/jazone_bg.png
